FROM golang:latest

MAINTAINER airdb.com

ENV NOTVISIBLE "in users profile"
RUN echo "export VISIBLE=now" >> /etc/profile

ENV RUNNING_CONTEXT "docker"
ENV echo "export RUNNING_CONTEXT=docker" >> /etc/profile

RUN echo "export PS1='[\H \W]\\$ '" >> /root/.profile

ENV NOTVISIBLE "in users profile"
RUN echo "export VISIBLE=now" >> /etc/profile

WORKDIR /srv
RUN apt-get update && \
	apt-get install -y curl && \
	apt-get install -y gcc g++ make less vim

RUN DEBIAN_FRONTEND=noninteractive apt-get install -y squid
